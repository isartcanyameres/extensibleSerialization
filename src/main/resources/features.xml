<?xml version="1.0" encoding="UTF-8"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0"
	name="ext-serialization">

	<!-- Apache CXF features repository -->
	<repository>mvn:org.apache.cxf.karaf/apache-cxf/${cxf-version}/xml/features</repository>

	<feature name="all" version="${project.version}">
	    <feature version="${cxf-version}">cxf</feature>
	    
		<bundle>mvn:net.i2cat.seg/base/${project.version}</bundle>
		<bundle>mvn:net.i2cat.seg/extension/${project.version}</bundle>
	</feature>
</features>